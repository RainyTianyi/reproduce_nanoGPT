# 改为使用TF32计算矩阵乘法时，训练速度问题

## 问题

在进行 git 提交注释为“Section2: 加速。添加训练时打印Batch计算时间。改为使用TF32计算矩阵乘法。此时4060一个Batch需要约4s。”这一步时，发现两个反常问题：

- 改变使用 TF32 和 BF32，速度几乎没有变化，都是约 980tok/sec。Deepseek V4.1 指出这个速度是异常慢的。
- 将 Dataloader 的参数从 B=4 调整到 B=8，速度几乎慢了 8 倍。和一般经验反常。


通过与 Deepseek 讨论，调用 Deepseek 完成关键计算：

---

B=8 在 4060 的 8GB 显存上确实装不下，Windows 没有报 OOM，是因为 NVIDIA 驱动启用了**共享 GPU 内存**（把一部分数据放到系统内存），导致数据在 PCIe 上反复搬运，速度暴跌。下面用具体计算验证。

### 1. 先算模型参数、梯度、优化器状态

模型是 GPT-2 124M 配置：

- 参数量：约 **124.4M**
- FP32 参数：124.4M × 4 = **497.8 MB**
- 梯度：同参数，**497.8 MB**
- AdamW 优化器状态：`exp_avg` + `exp_avg_sq`，各 497.8 MB，共 **995.6 MB**

**小计：约 1.99 GB ≈ 2.0 GB**

这部分与 batch 无关，B=4 和 B=8 都一样。

### 2. 再算激活值，重点是注意力矩阵

B=8, T=1024, C=768, n_head=12, head_dim=64。

每层注意力里最大的张量是注意力分数矩阵：

```text
att 形状: (B, n_head, T, T) = (8, 12, 1024, 1024)
元素数: 8 × 12 × 1024 × 1024 = 100,663,296
FP32 大小: 100,663,296 × 4 = 402.65 MB
```

Softmax 输出同样是 `(8, 12, 1024, 1024)`，**又是 402.65 MB**。

反向传播需要保存 softmax 输出，所以每层至少要留一个 402.65 MB 的注意力矩阵。12 层就是：

```text
402.65 MB × 12 = 4,831.8 MB ≈ 4.83 GB
```

**仅注意力 softmax 矩阵就 4.83 GB。**

### 3. 加上 LM Head 的 logits

```text
logits 形状: (B, T, vocab_size) = (8, 1024, 50257)
元素数: 8 × 1024 × 50257 = 411,041,792
FP32 大小: 411,041,792 × 4 = 1,644.2 MB ≈ 1.64 GB
```

`F.cross_entropy` 内部通常还会产生同样大的 `log_softmax` 中间张量，可能再占 1.64 GB，或者至少占用一部分。

### 4. 加上 MLP 和其他激活

每层 MLP 的 `c_fc` 输出：

```text
(B, T, 4C) = (8, 1024, 3072)
元素数: 25,165,824
FP32: 100.66 MB
```

GELU 输出同样 100.66 MB。12 层就是：

```text
(100.66 + 100.66) × 12 = 2,415.8 MB ≈ 2.42 GB
```

此外每层还有若干 `(B, T, C)` 的隐藏状态，每个 25.17 MB，12 层加起来约 **1 GB 左右**。

### 5. 总显存需求

| 项目 | 大小 |
|---|---|
| 参数 + 梯度 + AdamW | ~2.0 GB |
| 注意力 softmax 矩阵（12 层） | ~4.83 GB |
| MLP 激活（12 层） | ~2.42 GB |
| 其他隐藏状态（12 层） | ~1.0 GB |
| LM Head logits | ~1.64 GB |
| cross_entropy 中间 | ~0～1.64 GB |
| **合计** | **约 11.9～13.5 GB** |

而 4060 只有 **8 GB 专用显存**。所以 **B=8 必然超过专用显存上限**。

### 6. 为什么没报 OOM，而是变慢？

NVIDIA 驱动在 Windows 下有一个机制：当专用显存不足时，会自动使用**系统内存作为共享 GPU 内存**。这不会让 PyTorch 抛出 OOM，但会导致：

- 部分张量被放在系统内存
- 每次访问这些张量都要通过 **PCIe** 搬运
- PCIe 带宽远低于显存带宽（4060 显存 272 GB/s，PCIe 4.0 x8 约 16 GB/s）
- GPU 频繁等待数据，`sm` 显示 100%，但 `mem`（显存控制器利用率）只有 2%～3%
- 因为瓶颈在 PCIe，不在显存控制器

这正好解释了你看到的：

```text
sm 经常 100%，mem 长期 2%～3%
sm 在 100% 和 0% 之间剧烈抖动
B=8 比 B=4 更慢
TF32 开了没提升
```

`nvidia-smi dmon` 的 `mem` 只统计**专用显存带宽**，不统计 PCIe 流量，所以看起来显存很闲，实际数据在 PCIe 上堵着。

### 7. 结论

- **B=8 在 4060 8GB 上确实装不下**，估算需要 12～13 GB。
- Windows 没有报 OOM，而是用了共享系统内存，导致 PCIe 成为瓶颈，速度暴跌。
- 你的 `nvidia-smi dmon` 数据（`sm` 高、`mem` 极低、剧烈抖动）完全符合“显存溢出到系统内存”的特征。
- 解决方案：**B 降到 4 或更小**，或者用**梯度累积**模拟大 batch，同时开启 **AMP/bf16** 降低激活显存，有条件上 **WSL2/Linux**。

不是代码问题，也不是 TF32 问题，而是 **B=8 超出了 4060 的专用显存，Windows 用系统内存兜底，导致性能崩溃**。

---

## 实验

接下来运行 B=2 且启用 TF32 进行验证，以下为终端输出：

```
using device: cuda
loaded 338025 tokens
1 epoch = 165 batches
step 0, loss: 10.934523582458496, dt: 585.89ms, tok/sec: 3495.55
step 1, loss: 9.472288131713867, dt: 342.83ms, tok/sec: 5973.84
step 2, loss: 9.200361251831055, dt: 335.70ms, tok/sec: 6100.63
step 3, loss: 8.896467208862305, dt: 334.39ms, tok/sec: 6124.54
step 4, loss: 8.75160026550293, dt: 332.91ms, tok/sec: 6151.78
step 5, loss: 8.461453437805176, dt: 334.59ms, tok/sec: 6120.97
step 6, loss: 8.128312110900879, dt: 333.45ms, tok/sec: 6141.80
step 7, loss: 7.980883598327637, dt: 335.05ms, tok/sec: 6112.47
...
```

运行 B=2 且保留使用 FP32：

```
using device: cuda
loaded 338025 tokens
1 epoch = 165 batches
step 0, loss: 10.934581756591797, dt: 792.17ms, tok/sec: 2585.31
step 1, loss: 9.472172737121582, dt: 403.92ms, tok/sec: 5070.32
step 2, loss: 9.20007610321045, dt: 401.04ms, tok/sec: 5106.75
step 3, loss: 8.896628379821777, dt: 400.90ms, tok/sec: 5108.48
step 4, loss: 8.751441955566406, dt: 396.91ms, tok/sec: 5159.83
step 5, loss: 8.46125602722168, dt: 399.99ms, tok/sec: 5120.11
step 6, loss: 8.128143310546875, dt: 397.58ms, tok/sec: 5151.17
step 7, loss: 7.980915069580078, dt: 399.56ms, tok/sec: 5125.61
...
```

## 结论

1. **RTX4060 8GB 在训练 GPT2 124M 时，只能使用 2 的批量大小**。B=4 会超过一些，导致速度被 Windows 系统的共享内存机制（使用系统内存，PCIe称为瓶颈）卡死。
2. **Windows 系统存在诸多保护机制，使得 OOM 不那么容易被观察到**。同时各种教程建议使用 Linux 操作系统。后续需要考虑使用学校云端算力，或本地使用 WSL 在 Linux 系统下跑深度学习训练。
3. **B=2 时 TF32 带来的提升已经很不明显**（约 12% ），且对于训练来说太小了，不利于模型学习。需要进一步优化内存占用尽可能提高 B。
   

# 问题